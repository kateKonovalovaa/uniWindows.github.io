mixin build()
    section.build
        div.build__wrap.wrap
            div.build__content 
                h2.build__title.title Build it beautiful
                p.build__text Sometimes bold ideas don't require a full website. That's why Cover Pages can be used to convey a single idea in a beautiful way. 